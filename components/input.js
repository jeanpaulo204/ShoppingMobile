import React from 'react';
import { StyleSheet, Text, View, TextInput, Button }

const Input = () => {
    return (
       <View>
            <Text>Component</Text>
       </View>
        );
  };

export default Input;